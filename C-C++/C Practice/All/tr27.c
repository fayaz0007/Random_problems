#include <stdio.h>

int main()
{
    int grade;
    int ask;
    printf("plese enter your grade: ");
    scanf("%d",&ask);

    while(ask ==)
    {
      printf("plese enter your grade: ");
      scanf("%d",&grade);
      if (grade>0 || grade <= 100)
          printf("thanks");
      else
         continue;
      
    }
}